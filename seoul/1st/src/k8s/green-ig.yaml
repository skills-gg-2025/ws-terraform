apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: green-ingress
  namespace: wsk25
  annotations:
    alb.ingress.kubernetes.io/load-balancer-name: green-alb
    alb.ingress.kubernetes.io/target-type: instance
    alb.ingress.kubernetes.io/target-node-labels: role=app
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/healthcheck-path: /health
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - path: /green
            pathType: Exact
            backend:
              service:
                name: wsk-green-svc
                port:
                  number: 80
          - path: /health
            pathType: Exact
            backend:
              service:
                name: wsk-green-svc
                port:
                  number: 80